package com.gmail.nossr50.events.fake;

import org.bukkit.event.Event;

/**
 * This interface marks an {@link Event} as "fake". This is just a handy way of checking if an
 * {@link Event} is fake or not, maybe there will be methods suitable for this in the future.
 */
public interface FakeEvent {
}
